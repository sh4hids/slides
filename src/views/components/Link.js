import styled from "styled-components"
import { typography, color, space } from "styled-system"

const Link = styled.a`
  ${space};
  ${typography};
  ${color};

  color: #74b816;
`

export default Link
